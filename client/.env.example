# Leave empty in development (Vite proxy handles /api calls to localhost:3001)
# In production, set this to your deployed server URL (no trailing slash)
# e.g. https://quantum-drug-discovery-api.onrender.com
VITE_API_URL=
